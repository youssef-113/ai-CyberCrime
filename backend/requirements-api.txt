# ACEB Backend - API Container (slim)
# Only what the FastAPI process needs at import time + runtime

# ── Web Framework ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# ── HTTP Client ──
httpx>=0.25.0

# ── Authentication ──
PyJWT>=2.8.0
bcrypt>=4.1.0

# ── Database ──
supabase>=2.4.0

# ── Redis & Caching ──
redis>=5.0.0

# ── Celery (client only — tasks run on worker) ──
celery>=5.3.0
kombu>=5.3.0

# ── Image Processing (for OCR preprocessing at import time) ──
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
pdf2image>=1.16.0

# ── Arabic Text Processing ──
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# ── PDF Generation (needed at import time by pdf sub-app) ──
weasyprint>=60.0
jinja2>=3.1.0

# ── Security ──
python-magic>=0.4.27
pyclamd>=0.4.0
email-validator>=2.1.0

# ── Rate Limiting ──
slowapi>=0.1.9

# ── Multi-Agent (Verification) ──
langgraph>=0.2.0

# ── Logging & Monitoring ──
python-json-logger>=2.0.0
structlog>=24.0.0

# ── Utilities ──
python-dotenv>=1.0.0
tenacity>=8.0.0
orjson>=3.9.0
